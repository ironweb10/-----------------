/**
 * Represets an error thrown because a party already reached its max member count
 */
declare class PartyMaxSizeReachedError extends Error {
    constructor();
}
export default PartyMaxSizeReachedError;
